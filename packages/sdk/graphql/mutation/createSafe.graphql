mutation CreateSafe($input: CreateSafeInput!) {
  createSafe(input: $input) {
    document {
      safeAddress
      network
      totalTransactions
      singleton
      deployedBlockNumber
      nativeTokenBalance
      threshold
    }
  }
}